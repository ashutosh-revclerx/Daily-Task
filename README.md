# Daily-Task
current error 

INFO:     127.0.0.1:55307 - "POST /users/ HTTP/1.1" 500 Internal Server Error
ERROR:    Exception in ASGI application
this error occur because 2️⃣ What MongoDB does NOT support

❌ You cannot:

Use OAuth access tokens directly in MongoClient() like an API key

Replace username/password with Bearer <token> in a normal connection string

Use Google OAuth directly without OIDC federation
